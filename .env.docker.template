# Peach 1UP Docker configuration
# Copy to .env.docker and fill in values

# Network
PEACH_PORT=8080
ALLOW_NETWORK_ACCESS=false

# Paths (host paths mounted into containers)
IMAGES_PATH=./images
PROFILES_PATH=./profiles
