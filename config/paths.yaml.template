# Peach 1UP — paths.yaml template
#
# This file is for reference only. It is never loaded directly.
# The real paths.yaml is generated at first run in %APPDATA%\Peach1UP\paths.yaml
# and contains install-relative defaults. Edit that file to customise paths
# (e.g. library on a secondary drive or NAS share).
#
# All keys accept absolute paths only. Forward slashes on Windows are fine.

LIBRARY_PATH: ""
MEDIA_PATH: ""
OS_PATH: ""
ROMS_PATH: ""
PROFILES_PATH: ""
