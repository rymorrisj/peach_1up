slug = "86box-roms"
name = "86Box ROM Pack"
description = "ROM and firmware files required by 86Box for PC emulation"
version = "v5.3"
install_type = "rom_pack"
install_scope = "portable"
binary = "emulators/86box/roms"
# Inert for a rom_pack (it never launches as a Job Object process); set for
# schema uniformity since EmulatorDescriptor requires both fields.
skip_memory_limit = true
skip_cpu_limit = true
required = true
license = "Various"
source_url = "https://github.com/86Box/roms"
supported_formats = []
guidance_text = "Click Clone ROM Pack to download via git, or manually download and extract to emulators/86box/roms/."
guidance_url = "https://github.com/86Box/roms"
